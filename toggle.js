document.addEventListener("DOMContentLoaded", () => {
    const mode = localStorage.getItem("docMode");
    if (mode === "internal") {
        // redirect to internal docs if user is in public
    }
});