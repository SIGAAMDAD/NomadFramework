name: ⚡ Optimized CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 2 * * *'  # Run at 2 AM daily

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        dotnet-version: ['10.0.x', '8.0.x']
    
    runs-on: ${{ matrix.os }}
    timeout-minutes: 25
    
    env:
      DOTNET_CLI_TELEMETRY_OPTOUT: 1
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: ⚙️ Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ matrix.dotnet-version }}
        cache-depdendency-path: '**/*.csproj'
    
    - name: 🔒 Check for vulnerable packages
      run: dotnet list NomadFramework.slnx package --vulnerable --include-transitive
    
    - name: 💾 Cache build output
      uses: actions/cache@v4
      with:
        path: |
          ~/.local/share/NuGet
          ~/.nuget
          **/bin
          **/obj
        key: ${{ runner.os }}-build-${{ hashFiles('**/*.csproj') }}
        restore-keys: |
          ${{ runner.os }}-build-
    
    # WINDOWS-SPECIFIC OPTIMIZATIONS
    - name: 🪟 Windows optimizations
      if: runner.os == 'Windows'
      run: |
        # Disable Windows Defender during build (temporary)
        Set-MpPreference -DisableRealtimeMonitoring $true
        # Clean temp files
        Remove-Item -Path "$env:TEMP\*" -Recurse -Force -ErrorAction SilentlyContinue
        # Increase process priority
        $process = Get-Process -Id $PID
        $process.PriorityClass = [System.Diagnostics.ProcessPriorityClass]::High
    
    - name: 📦 Restore
      run: |
        dotnet restore NomadFramework.slnx

    - name: 🔍 Check code format
      run: dotnet format --verify-no-changes
    - name: 🔬 Run static analysis
      run: dotnet list package --vulnerable   # you already have this
      # Optionally add Roslyn analyzers: dotnet build -p:RunAnalyzers=true
    
    - name: 🔨 Build
      run: |
        dotnet build NomadFramework.slnx --configuration Release --no-restore
    
    - name: 🧪 Run tests with coverage
      run: |
        dotnet test --configuration Release --no-build --collect:"XPlat Code Coverage" --settings coverlet.runsettings

    - name: 📎 Upload test results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: test-results-${{ matrix.os }}-${{ matrix.dotnet-version }}
        path: ./TestResults
    
    - name: 📚 Generate Doxygen documentation
      run: doxygen Doxyfile   # assumes Doxyfile is in root
      continue-on-error: true # optional

    - name: 🚀 Deploy docs to Wiki
      if: github.ref == 'refs/heads/main'
      run: |
        git clone https://github.com/${{ github.repository }}.wiki.git ./wiki
        cp -r ./Documentation/html/* ./wiki/
        cd wiki
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add .
        git commit -m "Update Wiki documentation from CI"
        git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.wiki.git
      